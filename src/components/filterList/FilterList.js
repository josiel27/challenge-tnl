import React, { useState } from "react";

const FilterList = props => {

  

  return (
    <div>
      {/* <input
        type="text"
        value={filterStr}
        onChange={e => setFilterStr({ filterStr: e.target.value })}
      />
      <ul>{filteredElements}</ul> */}
    </div>
  );
};

export default FilterList;
